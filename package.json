{
  "name": "ground-control-frontend-pages",
  "private": true,
  "scripts": {
    "build:pages": "node scripts/build-pages.mjs",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.91.0"
  }
}
